---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-fast
  namespace: atom-intents
  labels:
    app: go-fast
spec:
  replicas: 2
  selector:
    matchLabels:
      app: go-fast
  template:
    metadata:
      labels:
        app: go-fast
    spec:
      serviceAccountName: go-fast
      containers:
        - name: go-fast
          image: SKIP_SELECT_IMAGE
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: SKIP_SELECT_PORT
              value: "8080"
            - name: AUCTION_INTERVAL_MS
              value: "500"
            - name: MOCK_LATENCY_MS
              value: "100"
            - name: ENABLE_ANALYTICS
              value: "true"
            - name: RUST_LOG
              value: "info,go_fast_simulator=debug"
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: go-fast
  namespace: atom-intents
spec:
  selector:
    app: go-fast
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: go-fast
  namespace: atom-intents
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: go-fast
  namespace: atom-intents
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: go-fast
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
